const $=s=>document.querySelector(s);let all=[];
async function json(url,options){const r=await fetch(url,options);const d=await r.json();if(!r.ok)throw Error(d.detail||`Request failed (${r.status})`);return d}
async function health(){try{const h=await json('/api/health');$('#apiStatus').innerHTML='<span></span> API '+h.status;$('#uptime').textContent=h.uptime_seconds+' seconds';$('#apiPreview').textContent=JSON.stringify(h,null,2)}catch(e){$('#apiStatus').textContent='● API unavailable'}}
function refreshFilters(){const cats=[...new Set(all.map(x=>x.category))].sort();$('#domainCount').textContent=cats.length;$('#filter').innerHTML='<option value="">All domains</option>'+cats.map(x=>`<option>${x}</option>`).join('')}
function render(items){$('#cards').innerHTML=items.map(x=>`<article class="card"><span class="tag">${x.category} · ${x.difficulty}</span><h3>${x.title}</h3><p>${x.answer.length} expected item${x.answer.length===1?'':'s'}</p><button data-id="${x.id}">Run benchmark →</button></article>`).join('');document.querySelectorAll('.card button').forEach(b=>b.onclick=()=>openRun(b.dataset.id))}
function openRun(id){const x=all.find(x=>x.id===id);$('#runner').classList.remove('hidden');$('#runCategory').textContent=x.category.toUpperCase()+' · '+x.difficulty.toUpperCase();$('#runTitle').textContent=x.title;$('#runPrompt').textContent=x.prompt;$('#answer').value='';$('#result').textContent='';$('#expected').innerHTML=x.answer.map(a=>'<li>'+a+'</li>').join('');$('#score').onclick=async()=>{try{const d=await json('/api/benchmarks/'+id+'/score',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({answer:$('#answer').value})});$('#result').textContent=`${d.score}% · ${d.correct} of ${d.total}`}catch(e){$('#result').textContent=e.message}};$('#askModel').onclick=async()=>{const b=$('#askModel');b.disabled=true;b.textContent='Calling model...';try{const d=await json('/api/benchmarks/'+id+'/run-model',{method:'POST'});$('#answer').value=d.answer;$('#result').textContent='Model response loaded—ready to score.'}catch(e){$('#result').textContent=e.message}finally{b.disabled=false;b.textContent='Ask selected model'}}}
async function loadSettings(){const c=await json('/api/config');$('#atlasUrl').value=c.atlas_url;$('#modelUrl').value=c.model_endpoint;$('#modelName').innerHTML=`<option value="${c.model_name}">${c.model_name||'Discover models first'}</option>`}
async function discover(){const status=$('#settingsResult');status.textContent='Looking for models...';try{await save(false);const d=await json('/api/models');$('#modelName').innerHTML=d.models.length?d.models.map(m=>`<option value="${m}" ${m===d.selected?'selected':''}>${m}</option>`).join(''):'<option value="">No installed models found</option>';status.textContent=`${d.models.length} ${d.provider} model${d.models.length===1?'':'s'} found.`}catch(e){status.textContent=e.message}}
async function save(validate=true){const d=await json('/api/config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({atlas_url:$('#atlasUrl').value,model_endpoint:$('#modelUrl').value,model_name:$('#modelName').value})});if(validate){$('#settingsResult').textContent='Saved. Atlas connection validated.';await loadBenchmarks()}return d}
async function loadBenchmarks(){try{const d=await json('/api/benchmarks');all=d.items;$('#count').textContent=d.count;refreshFilters();render(all)}catch(e){$('#cards').innerHTML=`<p class="error">Could not load benchmarks: ${e.message}</p>`}}
$('#settingsToggle').onclick=async()=>{$('#settings').classList.toggle('hidden');if(!$('#settings').classList.contains('hidden')){await loadSettings();discover()}};$('#discoverModels').onclick=discover;$('#saveSettings').onclick=()=>save(true).catch(e=>$('#settingsResult').textContent=e.message);$('#filter').onchange=e=>render(all.filter(x=>!e.target.value||x.category===e.target.value));$('#close').onclick=()=>$('#runner').classList.add('hidden');$('#startApi').onclick=health;(async()=>{health();loadBenchmarks();setInterval(health,12000)})()
