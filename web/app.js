const $=s=>document.querySelector(s);let all=[],pollTimer,activeRun,latestRun,modelDetails=[];
async function json(url,options){const r=await fetch(url,options);const d=await r.json();if(!r.ok)throw Error(d.detail||`Request failed (${r.status})`);return d}
async function health(){try{const h=await json('/api/health');$('#apiStatus').innerHTML='<span></span> API '+h.status;$('#uptime').textContent=h.uptime_seconds+' seconds';$('#apiPreview').textContent=JSON.stringify(h,null,2)}catch(e){$('#apiStatus').textContent='● API unavailable'}}
function categories(){return[...new Set(all.map(x=>x.category))].sort()}
function refreshFilters(){const cats=categories();$('#domainCount').textContent=cats.length;$('#filter').innerHTML='<option value="">All domains</option>'+cats.map(x=>`<option>${x}</option>`).join('')}
function render(selected=''){const cats=categories().filter(x=>!selected||x===selected);$('#cards').innerHTML=cats.map(category=>{const items=all.filter(x=>x.category===category);return`<article class="card"><span class="tag">${category.toUpperCase()}</span><h3>${category}</h3><p>${items.length} questions · one complete benchmark run</p><button data-category="${category}">Run all ${items.length} →</button></article>`}).join('');document.querySelectorAll('[data-category]').forEach(b=>b.onclick=()=>startRun(b.dataset.category))}
function openRunner(category){$('#runner').classList.remove('hidden');$('#runCategory').textContent=category.toUpperCase();$('#runTitle').textContent=`${category} benchmark`;$('#runResults').innerHTML='';$('#progressBar').style.width='0%'}
async function startRun(category){clearTimeout(pollTimer);openRunner(category);$('#runProgress').textContent='Starting model run...';try{const job=await json('/api/runs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({category})});activeRun=job.id;pollRun(job.id)}catch(e){$('#runProgress').textContent=e.message}}
function displayJob(job){latestRun=job;const percent=job.total?Math.round(job.completed/job.total*100):0;$('#progressBar').style.width=percent+'%';$('#runProgress').textContent=job.status==='complete'?`Complete · ${job.completed} questions scored`:`${job.completed} / ${job.total} questions · ${percent}% complete`;$('#metricScore').textContent=job.score+'%';$('#metricCorrect').textContent=job.correct;$('#metricIncorrect').textContent=job.incorrect;$('#metricLatency').textContent=job.average_latency_ms+' ms';$('#metricThroughput').textContent=job.questions_per_minute;$('#result').textContent=`Model: ${job.model} · Status: ${job.status}`;$('#exportRun').disabled=!job.results.length;$('#cancelRun').disabled=!['queued','running'].includes(job.status);$('#runResults').innerHTML=job.results.slice().reverse().map(row=>`<div class="result-row ${row.score===100?'pass':'fail'}"><b>${row.score}%</b><span>${row.prompt}</span><small>Response: ${row.response||row.error||'No response'} · Expected: ${row.expected.join(', ')} · ${row.latency_ms} ms</small></div>`).join('')}
async function pollRun(id){try{const job=await json('/api/runs/'+id);displayJob(job);if(['queued','running'].includes(job.status))pollTimer=setTimeout(()=>pollRun(id),1000);else loadHistory()}catch(e){$('#runProgress').textContent=e.message}}
async function viewRun(id){clearTimeout(pollTimer);const job=await json('/api/runs/'+id);activeRun=id;openRunner(job.category);displayJob(job)}
async function loadHistory(){try{const d=await json('/api/runs');$('#runHistory').innerHTML=d.items.length?d.items.map(job=>`<button class="history-card" data-run="${job.id}"><span>${job.category}</span><b>${job.score}%</b><small>${job.model} · ${job.completed}/${job.total} · ${job.status}</small></button>`).join(''):'<p>No benchmark runs yet.</p>';document.querySelectorAll('[data-run]').forEach(b=>b.onclick=()=>viewRun(b.dataset.run))}catch(e){$('#runHistory').innerHTML=`<p>${e.message}</p>`}}
async function loadSettings(){const c=await json('/api/config');$('#atlasUrl').value=c.atlas_url;$('#modelUrl').value=c.model_endpoint;$('#modelName').value=c.model_name||''}
function showModelMeta(){const d=modelDetails.find(x=>x.name===$('#modelName').value);$('#modelMeta').innerHTML=d?`<b>${d.name}</b><span>${[d.parameters,d.quantization,d.family,d.size?Math.round(d.size/1e6)+' MB':''].filter(Boolean).join(' · ')}</span>`:''}
async function save(validate=true){const d=await json('/api/config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({atlas_url:$('#atlasUrl').value,model_endpoint:$('#modelUrl').value,model_name:$('#modelName').value})});if(validate){$('#settingsResult').textContent='Saved. Atlas connection validated.';await loadBenchmarks()}return d}
async function discover(){const status=$('#settingsResult');status.textContent='Looking for models...';try{await save(false);const d=await json('/api/models');modelDetails=d.details||[];$('#modelOptions').innerHTML=d.models.map(m=>`<option value="${m}"></option>`).join('');if(!$('#modelName').value&&d.models.length)$('#modelName').value=d.models[0];status.textContent=d.warning||`${d.models.length} ${d.provider} models found.`;showModelMeta()}catch(e){status.textContent=e.message+' You can still type the model name manually and save.'}}
async function loadBenchmarks(){try{const d=await json('/api/benchmarks');all=d.items;$('#count').textContent=d.count;refreshFilters();render()}catch(e){$('#cards').innerHTML=`<p class="error">Could not load benchmarks: ${e.message}</p>`}}
$('#settingsToggle').onclick=async()=>{$('#settings').classList.toggle('hidden');if(!$('#settings').classList.contains('hidden')){await loadSettings();discover()}};$('#discoverModels').onclick=discover;$('#modelName').onchange=showModelMeta;$('#saveSettings').onclick=()=>save(true).catch(e=>$('#settingsResult').textContent=e.message);$('#filter').onchange=e=>render(e.target.value);$('#close').onclick=()=>{$('#runner').classList.add('hidden');clearTimeout(pollTimer)};$('#cancelRun').onclick=()=>activeRun&&json('/api/runs/'+activeRun,{method:'DELETE'}).then(()=>pollRun(activeRun));$('#exportRun').onclick=()=>{if(!latestRun)return;const blob=new Blob([JSON.stringify(latestRun,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`benchmark-${latestRun.category}-${latestRun.model}.json`;a.click();URL.revokeObjectURL(a.href)};$('#startApi').onclick=health;(async()=>{health();loadBenchmarks();loadHistory();setInterval(health,12000)})()
