const $=s=>document.querySelector(s);let all=[];
async function health(){try{const h=await(await fetch('/api/health')).json();$('#apiStatus').innerHTML='<span></span> API '+h.status;$('#uptime').textContent=h.uptime_seconds+' seconds';$('#apiPreview').textContent=JSON.stringify(h,null,2)}catch(e){$('#apiStatus').textContent='● API unavailable'}}
function render(items){$('#cards').innerHTML=items.map(x=>`<article class="card"><span class="tag">${x.category} · ${x.difficulty}</span><h3>${x.title}</h3><p>${x.answer.length} expected items</p><button data-id="${x.id}">Run benchmark →</button></article>`).join('');document.querySelectorAll('.card button').forEach(b=>b.onclick=()=>openRun(b.dataset.id))}
function openRun(id){let x=all.find(x=>x.id===id);$('#runner').classList.remove('hidden');$('#runCategory').textContent=x.category.toUpperCase()+' · '+x.difficulty.toUpperCase();$('#runTitle').textContent=x.title;$('#runPrompt').textContent=x.prompt;$('#answer').value='';$('#result').textContent='';$('#expected').innerHTML=x.answer.map(a=>'<li>'+a+'</li>').join('');$('#score').onclick=async()=>{let d=await(await fetch('/api/benchmarks/'+id+'/score',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({answer:$('#answer').value})})).json();$('#result').textContent=`${d.score}% · ${d.correct} of ${d.total}`};$('#askModel').onclick=async()=>{let b=$('#askModel');b.textContent='Calling model…';try{let d=await(await fetch('/api/benchmarks/'+id+'/run-model',{method:'POST'})).json();if(d.detail)throw Error(d.detail);$('#answer').value=d.answer;$('#result').textContent='Model response loaded — score it.'}catch(e){$('#result').textContent=e.message}finally{b.textContent='Ask model'}}}
async function settings(){let c=await(await fetch('/api/config')).json();$('#atlasUrl').value=c.atlas_url;$('#modelUrl').value=c.model_endpoint}$('#settingsToggle').onclick=()=>{$('#settings').classList.toggle('hidden');settings()};$('#saveSettings').onclick=async()=>{let r=await fetch('/api/config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({atlas_url:$('#atlasUrl').value,model_endpoint:$('#modelUrl').value})});let d=await r.json();$('#settingsResult').textContent=d.detail||'Saved and Atlas validated.';if(!d.detail){let b=await(await fetch('/api/benchmarks')).json();all=b.items;$('#count').textContent=b.count;render(all)}};
$('#filter').onchange=e=>render(all.filter(x=>!e.target.value||x.category===e.target.value));$('#close').onclick=()=>$('#runner').classList.add('hidden');$('#startApi').onclick=async()=>{await fetch('/api/control/start',{method:'POST'});$('#startApi').textContent='API online ✓';health()};(async()=>{health();let d=await(await fetch('/api/benchmarks')).json();all=d.items;$('#count').textContent=d.count;render(all);setInterval(health,12000)})()
