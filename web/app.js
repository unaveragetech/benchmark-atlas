const $=s=>document.querySelector(s);let all=[];let pollTimer;
async function json(url,options){const r=await fetch(url,options);const d=await r.json();if(!r.ok)throw Error(d.detail||`Request failed (${r.status})`);return d}
async function health(){try{const h=await json('/api/health');$('#apiStatus').innerHTML='<span></span> API '+h.status;$('#uptime').textContent=h.uptime_seconds+' seconds';$('#apiPreview').textContent=JSON.stringify(h,null,2)}catch(e){$('#apiStatus').textContent='● API unavailable'}}
function categories(){return[...new Set(all.map(x=>x.category))].sort()}
function refreshFilters(){const cats=categories();$('#domainCount').textContent=cats.length;$('#filter').innerHTML='<option value="">All domains</option>'+cats.map(x=>`<option>${x}</option>`).join('')}
function render(selected=''){const cats=categories().filter(x=>!selected||x===selected);$('#cards').innerHTML=cats.map(category=>{const items=all.filter(x=>x.category===category);return`<article class="card"><span class="tag">${category.toUpperCase()}</span><h3>${category}</h3><p>${items.length} questions · one complete benchmark run</p><button data-category="${category}">Run all ${items.length} →</button></article>`}).join('');document.querySelectorAll('[data-category]').forEach(b=>b.onclick=()=>startRun(b.dataset.category))}
async function startRun(category){clearTimeout(pollTimer);$('#runner').classList.remove('hidden');$('#runCategory').textContent=category.toUpperCase();$('#runTitle').textContent=`${category} benchmark`;$('#runProgress').textContent='Starting model run...';$('#result').textContent='';$('#progressBar').style.width='0%';$('#runResults').innerHTML='';try{const job=await json('/api/runs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({category})});pollRun(job.id)}catch(e){$('#runProgress').textContent=e.message}}
async function pollRun(id){try{const job=await json('/api/runs/'+id);const percent=job.total?Math.round(job.completed/job.total*100):0;$('#progressBar').style.width=percent+'%';$('#runProgress').textContent=`${job.completed} / ${job.total} questions · ${percent}% complete`;$('#result').textContent=`Current score: ${job.score}% · ${job.model}`;$('#runResults').innerHTML=job.results.slice(-10).reverse().map(row=>`<div class="result-row ${row.score===100?'pass':'fail'}"><b>${row.score}%</b><span>${row.prompt}</span><small>${row.response||row.error||'No response'}</small></div>`).join('');if(job.status!=='complete')pollTimer=setTimeout(()=>pollRun(id),1000);else $('#runProgress').textContent=`Complete · ${job.completed} questions scored`}catch(e){$('#runProgress').textContent=e.message}}
async function loadSettings(){const c=await json('/api/config');$('#atlasUrl').value=c.atlas_url;$('#modelUrl').value=c.model_endpoint;$('#modelName').innerHTML=`<option value="${c.model_name}">${c.model_name||'Discover models first'}</option>`}
async function save(validate=true){const d=await json('/api/config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({atlas_url:$('#atlasUrl').value,model_endpoint:$('#modelUrl').value,model_name:$('#modelName').value})});if(validate){$('#settingsResult').textContent='Saved. Atlas connection validated.';await loadBenchmarks()}return d}
async function discover(){const status=$('#settingsResult');status.textContent='Looking for models...';try{await save(false);const d=await json('/api/models');$('#modelName').innerHTML=d.models.length?d.models.map(m=>`<option value="${m}" ${m===d.selected?'selected':''}>${m}</option>`).join(''):'<option value="">No installed models found</option>';status.textContent=`${d.models.length} ${d.provider} models found.`}catch(e){status.textContent=e.message}}
async function loadBenchmarks(){try{const d=await json('/api/benchmarks');all=d.items;$('#count').textContent=d.count;refreshFilters();render()}catch(e){$('#cards').innerHTML=`<p class="error">Could not load benchmarks: ${e.message}</p>`}}
$('#settingsToggle').onclick=async()=>{$('#settings').classList.toggle('hidden');if(!$('#settings').classList.contains('hidden')){await loadSettings();discover()}};$('#discoverModels').onclick=discover;$('#saveSettings').onclick=()=>save(true).catch(e=>$('#settingsResult').textContent=e.message);$('#filter').onchange=e=>render(e.target.value);$('#close').onclick=()=>{$('#runner').classList.add('hidden');clearTimeout(pollTimer)};$('#startApi').onclick=health;(async()=>{health();loadBenchmarks();setInterval(health,12000)})()
