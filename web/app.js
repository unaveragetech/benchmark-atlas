const $=s=>document.querySelector(s); let all=[];
async function health(){try{const h=await (await fetch('/api/health')).json();$('#apiStatus').innerHTML='<span></span> API '+h.status;$('#uptime').textContent=h.uptime_seconds+' seconds';$('#apiPreview').textContent=JSON.stringify(h,null,2)}catch(e){$('#apiStatus').textContent='● API unavailable'}}
function render(items){$('#cards').innerHTML=items.map(x=>`<article class="card"><span class="tag">${x.category} · ${x.difficulty}</span><h3>${x.title}</h3><p>${x.answer.length} expected items</p><button data-id="${x.id}">Run benchmark →</button></article>`).join('');document.querySelectorAll('.card button').forEach(b=>b.onclick=()=>openRun(b.dataset.id))}
function openRun(id){let x=all.find(x=>x.id===id);$('#runner').classList.remove('hidden');$('#runCategory').textContent=x.category.toUpperCase()+' · '+x.difficulty.toUpperCase();$('#runTitle').textContent=x.title;$('#runPrompt').textContent=x.prompt;$('#answer').value='';$('#result').textContent='';$('#expected').innerHTML=x.answer.map(a=>'<li>'+a+'</li>').join('');$('#score').onclick=async()=>{let r=await fetch('/api/benchmarks/'+id+'/score',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({answer:$('#answer').value})});let d=await r.json();$('#result').textContent=`${d.score}% · ${d.correct} of ${d.total} in exact sequence`}}
$('#filter').onchange=e=>render(all.filter(x=>!e.target.value||x.category===e.target.value));$('#close').onclick=()=>$('#runner').classList.add('hidden');$('#startApi').onclick=async()=>{let d=await (await fetch('/api/control/start',{method:'POST'})).json();$('#startApi').textContent='API online ✓';health()};
(async()=>{health();let d=await (await fetch('/api/benchmarks')).json();all=d.items;$('#count').textContent=d.count;render(all);setInterval(health,12000)})()
